순수 로직 (I9). Node와 Electron API를 import하지 않는다. `eslint.config.mjs`가 막는다.
시험은 `test/unit/`의 [단위]이고 Linux 러너에서 돈다.

| 모듈       | 맡는 일                                                                    | 설계                  |
| ---------- | -------------------------------------------------------------------------- | --------------------- |
| `pipeline` | 노드 순서, S 빠른 경로, 선택 가능한 다음 단계, 기본 다음 단계              | 3.1, 3.2, 3.4         |
| `machine`  | Work와 Task 상태 전이. `(상태, 이벤트) → (새 상태, 할 일)` (I10)           | 3.3, 시나리오 2~5     |
| `validate` | handoff와 intent 초안의 머리글 파싱, 스키마 검사, 추가 검사, 되돌림 메시지 | 5.2.1, D107           |
| `context`  | `context.md` 조립, 마무리 안내 문구                                        | 시나리오 2-4, D104    |
| `settings` | task 설정 파일 내용(훅, deny 규칙), 실행 인자                              | 시나리오 2-3·2-5, 6절 |
