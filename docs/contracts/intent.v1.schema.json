{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://relay.local/schemas/intent.v1.json",
  "title": "Intent front matter (v1)",
  "description": "works/<work-id>/intent.md 의 YAML 머리글. 본문 절 규칙은 x-relay-body에 적고, 앱이 머리글과 별도로 검사한다.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "version", "title", "type", "size", "delivery"],
  "properties": {
    "schema_version": { "const": 1 },
    "version": {
      "type": "integer",
      "minimum": 1,
      "description": "앱이 승인 시점에 매긴다. 초안(intent.draft.md)에서는 에이전트가 현재 버전+1을 적는다."
    },
    "title": {
      "type": "string",
      "minLength": 1,
      "maxLength": 80,
      "description": "사이드바와 브랜치 설명, PR 제목 기본값에 쓰는 한 줄 제목."
    },
    "type": {
      "enum": ["bugfix", "feature", "refactor", "analysis"],
      "description": "v1은 bugfix 파이프라인만 있다. 파이프라인은 Work 생성 시 사용자가 고르고, 이 값은 그 파이프라인의 applies_to.type과 같아야 한다."
    },
    "size": {
      "enum": ["S", "M", "L"],
      "description": "파이프라인 노드의 when 조건이 참조한다. S는 evidence/rca를 건너뛴다. work-start가 제안하고 g-intent에서 사용자가 확정한다."
    },
    "delivery": {
      "enum": ["none", "push", "pr"],
      "description": "Work 완료 시 앱이 수행할 전달 동작 (D15, D19)."
    }
  },
  "x-relay-body": {
    "max_chars": 1500,
    "sections": [
      { "heading": "목표", "required": true },
      { "heading": "비목표", "required": true, "note": "없으면 '없음'이라고 적는다" },
      { "heading": "원하는 결과", "required": true },
      { "heading": "완료조건", "required": true, "note": "'- [ ] '로 시작하는 목록. 항목마다 검증 가능한 한 문장. 최소 1개" },
      { "heading": "제약", "required": false },
      { "heading": "추가 의견", "required": false }
    ]
  }
}
